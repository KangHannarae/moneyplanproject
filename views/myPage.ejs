<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>머니플래너</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link rel="stylesheet" type="text/css" href="/stylesheets/main.css">
  <link rel="stylesheet" type="text/css" href="/stylesheets/myPage.css">
  <script>
    function goFavList(userid) {
      location.href = "/money/user_favlist/" + userid;
    }
    function goPostList(userid) {
      location.href = "/money/user_postlist/" + userid;
    }
    function goCommentList(userid) {
      location.href = "/money/user_commentlist/" + userid;
    }
    function goMyUpdate(userid) {
      location.href = "/money/userdetail/" + userid;
    }
  </script>
</head>
<body>
  <%- include('nav.ejs', { session: session }) %>

  <div class="mypage-wrapper">
    <div class="mypage-container">
      <div class="username-box"><%= session.username %>님의 마이페이지</div>

      <button class="btn-custom" onclick="goFavList('<%= session.userid %>')">
        <span class="btn-icon">⭐</span> 즐겨찾기 목록
      </button>
      <button class="btn-custom" onclick="goPostList('<%= session.userid %>')">
        <span class="btn-icon">📝</span> 작성 게시글 목록
      </button>
      <button class="btn-custom" onclick="goCommentList('<%= session.userid %>')">
        <span class="btn-icon">💬</span> 작성 댓글 목록
      </button>
      <button class="btn-custom" onclick="goMyUpdate('<%= session.userid %>')">
        <span class="btn-icon">⚙️</span> 회원 정보 수정
      </button>
    </div>
  </div>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
