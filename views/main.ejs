<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>머니플래너</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link rel="stylesheet" type="text/css" href="/stylesheets/main.css">
</head>

<body>

  <!-- 네비게이션 바 -->
  <%- include('nav.ejs') %>
  <!-- 메인 컨텐츠 -->
  <% if(session.userid != null) { %>
    <!-- 시작하기 섹션 -->
    <section class="start-section">
      <h1><%=session.username%>님 환영합니다.</h1>
      <button class="btn btn-login" onclick="location.href='/money/logout'">LOGOUT</button>
      <% if(session.userid == 'admin') { %>
        <a href="/money/adminPage" class="register-link">관리자 페이지</a>
      <% } else { %>
        <a href="/money/moneylist" class="register-link">내 가계부</a>
      <% } %>
    </section>
  <% } else { %>
      <section class="start-section">
        <h1>머니 플랜 시작하기</h1>
        <button class="btn btn-login" onclick="location.href='/money/login'">LOGIN</button>
        <a href="/money/join" class="register-link">계정 등록</a>
      </section>
  <% } %>
  <!-- 캐러셀 섹션 -->
  <section class="carousel-section">
    <div class="container d-flex justify-content-center">
      <div id="carouselExampleAutoplaying" class="carousel slide" data-bs-ride="carousel" style="max-width: 600px; width: 100%;">
        <div class="carousel-inner">
          <div class="carousel-item active">
            <img src="/images/1.png" class="d-block w-100 carousel-image" alt="...">
          </div>
          <div class="carousel-item">
            <img src="/images/2.png" class="d-block w-100 carousel-image" alt="...">
          </div>
          <div class="carousel-item">
            <img src="/images/3.png" class="d-block w-100 carousel-image" alt="...">
          </div>
        </div>
        <button class="carousel-control-prev" type="button" data-bs-target="#carouselExampleAutoplaying" data-bs-slide="prev">
          <span class="carousel-control-prev-icon" aria-hidden="true"></span>
          <span class="visually-hidden">Previous</span>
        </button>
        <button class="carousel-control-next" type="button" data-bs-target="#carouselExampleAutoplaying" data-bs-slide="next">
          <span class="carousel-control-next-icon" aria-hidden="true"></span>
          <span class="visually-hidden">Next</span>
        </button>
      </div>
    </div>
  </section>

 <!-- footer -->
 <%-include('footer.ejs') %>

  <!-- 부트스트랩 JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
   <script>
    var msg = "<%= msg %>";
    if (msg === "bye") {
      alert("회원 탈퇴가 정상적으로 이루어졌습니다 :)");
    }
  </script>
</body>
</html>
