<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>머니플래너</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link rel="stylesheet" type="text/css" href="/stylesheets/diarylist.css">
  <link rel="stylesheet" type="text/css" href="/stylesheets/main.css">
</head>
<body>
  <%-include('nav.ejs')%>
  <div class="diary-wrapper">
    <div class="diary-container">
      <h2>소비 메모</h2>
      <table>
        <tr>
          <th>날짜</th>
          <th>사용처</th>
          <th>메모</th>
          <th>사진</th>
        </tr>
        <% for (var i = 0; i < diaries.length; i++) { 
            var item = diaries[i];
            if (item != null) {
        %>
        <tr>
          <td><%= item.date %></td>
          <td><%= item.contents %></td>
          <td>
            <a href="moneydetail/<%= item.idx %>">
              <%= item.diary %>
            </a>
          </td>
          <td>
            <% if(item.diary_img !== '-') { %>
              <img src="/images/<%= item.diary_img %>" width="100px" height="100px">
            <% } %>
          </td>
        </tr>
        <% }} %>
      </table>
    </div>
  </div>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>
